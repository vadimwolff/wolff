/* ==========================================================================
 * WolffMsg — конфигурация подключения
 *
 * Приложение работает с любым PostgREST-совместимым адресом (Supabase REST).
 * Список ниже перебирается сверху вниз: первый ответивший адрес становится
 * рабочим и запоминается. Это нужно, чтобы сайт продолжал работать из сетей,
 * где часть доменов недоступна.
 * ========================================================================== */

window.WM_CONFIG = {
    /* Ключ базы нужен только прямым адресам (последние два в списке). Когда
       приложение работает через свой сервер (/api/db), ключ не отправляется
       вовсе: сервер подставляет его сам, и в браузере его нет.

       Чтобы убрать ключ из браузера совсем, смените его в Supabase
       (Settings → API → rotate anon key), пропишите новый только в настройках
       Vercel и оставьте здесь пустую строку — тогда останется единственный
       путь: через свой сервер. */
    apiKey: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InJ6Z2l5YWZwa2VxdmJwcW9tYmtyIiwicm9sZSI6ImFub24iLCJpYXQiOjE3Nzg3NzU5MTksImV4cCI6MjA5NDM1MTkxOX0.qvKNRcO-ylrWzOFYxEvWhcGBeSCxoanZx4i1VnhF7_w",

    endpoints: [
        {
            /* Работает, когда сайт развёрнут на Vercel: запрос идёт на тот же
               домен, что и сайт, и уже сервер обращается к базе. Самый надёжный
               вариант для России — сторонние домены не задействуются. */
            url: "same-origin:/api/db",
            label: "Через сервер сайта"
        },
        {
            url: "https://myproxy.vadimwolff2000.workers.dev/rest/v1",
            label: "Резервный канал"
        },
        {
            url: "https://rzgiyafpkeqvbpqombkr.supabase.co/rest/v1",
            label: "Основной сервер"
        }
    ],

    /* ГЛАВНОЕ ПОЛЕ, ЕСЛИ САЙТ И СЕРВЕР ЖИВУТ НА РАЗНЫХ АДРЕСАХ.

       Адрес вашего проекта на Vercel — целиком, например
       "https://wolffmsg.vercel.app" (без /api и без косой черты в конце).

       Там живут помощник WolffAI (/api/ai), рассылка уведомлений при закрытом
       приложении (/api/push) и поиск гифок (/api/gif). Когда сайт открыт с
       того же адреса, поле можно оставить пустым — приложение найдёт всё само.
       А вот если сайт открывают с GitHub Pages (адрес вида
       https://логин.github.io/репозиторий/), то серверных функций на нём нет,
       и без этого поля помощник не найдётся.

       Заполнили один раз — работает у всех и на всех устройствах. Можно и не
       заполнять: тогда адрес задаётся на каждом устройстве вручную в
       Настройки → Помощник WolffAI → Указать адрес. */
    serverUrl: "",

    /* Точечные адреса — нужны, только если службы разнесены по разным местам.
       Обычно достаточно serverUrl выше. */
    pushUrl: "",
    aiUrl: "",

    /* Серверы для звонков. STUN помогает двум устройствам найти друг друга
       через интернет. Если звонки не соединяются из некоторых сетей, сюда
       добавляют свой TURN-сервер: { urls, username, credential }. */
    iceServers: [
        { urls: "stun:stun.l.google.com:19302" },
        { urls: "stun:stun1.l.google.com:19302" },
        { urls: "stun:stun.cloudflare.com:3478" }
    ],

    /* Интервалы опроса сервера, мс */
    pollChatMs: 2000,
    pollListMs: 5000,

    /* Ограничения на загружаемые изображения */
    imageMaxSide: 1280,
    imageQuality: 0.72
};
